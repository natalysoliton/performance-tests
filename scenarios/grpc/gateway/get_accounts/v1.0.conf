# Конфигурация нагрузочного тестирования для сценария получения счетов
# Версия: 1.0
# Протокол: gRPC
# Сервис: grpc-gateway

# Путь до файла сценария
locustfile = ./scenarios/grpc/gateway/get_accounts/scenario.py

# Общее количество виртуальных пользователей
users = 300

# Скорость запуска пользователей (пользователей/секунду)
spawn-rate = 30

# Продолжительность теста (3 минуты)
run-time = 3m

# Запуск без веб-интерфейса
headless = true

# Путь для сохранения HTML-отчёта
html = ./scenarios/grpc/gateway/get_accounts/report.html

# Уровень логирования
loglevel = INFO

# Хост (фиктивное значение, так как клиент использует свой канал)
host = localhost